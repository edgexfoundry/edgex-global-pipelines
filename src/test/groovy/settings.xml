<settings>
  <proxies>
    <proxy>
      <id>internalproxy</id>
      <active>true</active>
      <protocol>http</protocol>
      <host>${env.PROXY_HOST}</host>
      <port>${env.PROXY_PORT}</port>
    </proxy>
 </proxies>
</settings>
